@model FamilyHub.Models.UserDeleteViewModel

@{
    ViewData["Title"] = "Lock User";
}

<div class="card border-0 shadow-sm rounded-4">
    <div class="card-body p-4">
        <h1 class="h3 fw-bold mb-3">Lock User</h1>
        <p class="text-muted">This will lock the user's account and prevent them from signing in.</p>

        <div class="border rounded-4 p-3 mb-4">
            <p class="mb-1 fw-semibold">@Model.FullName</p>
            <p class="mb-0 text-muted">@Model.Email</p>
        </div>

        <form asp-action="Lock" method="post">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-danger rounded-pill px-4">Lock Account</button>
                <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">Cancel</a>
            </div>
        </form>
    </div>
</div>
